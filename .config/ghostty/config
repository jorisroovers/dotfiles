# Split panes
keybind = cmd+t=new_split:right
keybind = cmd+shift+t=new_split:down

keybind = cmd+left=goto_split:left
keybind = cmd+right=goto_split:right

keybind = cmd+up=goto_split:top
keybind = cmd+down=goto_split:bottom
keybind = cmd+enter=toggle_split_zoom

# Emacs keybinds
# Cmd+C to Ctrl+C (interrupt)
keybind = cmd+c=text:\x03

# Cmd+A to Ctrl+A (beginning-of-line)
keybind = cmd+a=text:\x01

# Cmd+E to Ctrl+E (end-of-line)
keybind = cmd+e=text:\x05

# Cmd+U to Ctrl+U (kill-line)
keybind = cmd+u=text:\x15

# Cmd+R to Ctrl+R (reverse-search-history)
keybind = cmd+r=text:\x12

