#!/usr/bin/env bash
set -euo pipefail

printf 'casa-todo is deprecated; use notion-todo instead.\n' >&2
script_dir="$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)"
exec "$script_dir/notion-todo" "$@"
